#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int var = 48;
char tyo = '\n';
while (var < 68)
{
putchar(var);
var++;
}
putchar(tyo);
return (0);
}
